= content_for :js_for_head do
  :javascript
    Ciclavia.PageData = {
      routes: #{Route.all.map { |route| route.to_json } },
      midpoint: #{Route.last.midpoint}
    }

#map
.map-ciclavialogo
.mapnav-container
  - Route.all.each do |route|
    .mapnav-route{:data => { :id => route.id }}
      %span.mapnav-title= route.name
