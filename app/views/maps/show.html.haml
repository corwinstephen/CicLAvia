= content_for :js_for_head do
  :javascript
    Ciclavia.PageData = {
      routes: #{Route.all.map { |route| route.coordinate_array } },
      midpoint: #{Route.last.midpoint}
    }

#map
.map-ciclavialogo