.container
  .row
    .col-md-8
      %h1 Showing all maps
    .col-md-4
      = link_to "Create a new map", new_map_path, class: "btn btn-default"

  %hr

  .row
    .col-md-12
      %dl
        - Map.all.each do |m|
          %dt= link_to m.name, map_path(m)
          - if m.description.present?
            %dd= m.description
